# Charts Assets

Salva qui gli screenshot dei grafici per gli esercizi giornalieri.
Usa nomi coerenti con i campi `imageBefore` (grafico iniziale) e `imageAfter` (grafico spiegato)
definiti in `src/data/exercises.js`.
